# Vercel 部署环境变量配置
# 在 Vercel 控制台中设置这些环境变量

# Flask 应用密钥 - 在生产环境中必须设置
SECRET_KEY=your-secret-key-here

# 数据库配置 - 推荐使用 Vercel Postgres
# 在 Vercel 控制台中创建 Postgres 数据库后，会自动提供以下环境变量：
# POSTGRES_URL, POSTGRES_PRISMA_URL, POSTGRES_URL_NON_POOLING, POSTGRES_USER, POSTGRES_HOST, POSTGRES_PASSWORD, POSTGRES_DATABASE
# 将 POSTGRES_URL 设置为 DATABASE_URL

# Vercel Postgres 配置示例（自动设置）：
# DATABASE_URL=postgres://username:password@host:port/database?sslmode=require

# 如果需要手动配置其他数据库：
# PostgreSQL: postgresql://username:password@host:port/database?sslmode=require
# MySQL: mysql+pymysql://username:password@host:port/database
# SQLite（不推荐用于生产）：sqlite:///tmp/noteblog.db

# Flask 环境
FLASK_ENV=production

# 跳过插件初始化（在 serverless 环境中需要）
SKIP_PLUGIN_INIT=1

# 其他可选配置
# MAX_CONTENT_LENGTH=16777216
# UPLOAD_FOLDER=uploads
# ALLOWED_EXTENSIONS=png,jpg,jpeg,gif
