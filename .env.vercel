# Vercel 部署环境变量配置
# 在 Vercel 控制台中设置这些环境变量

# Flask 应用密钥 - 在生产环境中必须设置
SECRET_KEY=your-secret-key-here

# 数据库配置 - 默认使用 SQLite
# 对于 Vercel 部署，建议使用 SQLite 作为简单部署方案
# 如需使用其他数据库，请替换为相应的连接字符串
# SQLite: sqlite:///tmp/noteblog.db
# PostgreSQL: postgresql://username:password@host:port/database
# MySQL: mysql+pymysql://username:password@host:port/database
DATABASE_URL=sqlite:///tmp/noteblog.db

# Flask 环境
FLASK_ENV=production

# 跳过插件初始化（在 serverless 环境中需要）
SKIP_PLUGIN_INIT=1

# 其他可选配置
# MAX_CONTENT_LENGTH=16777216
# UPLOAD_FOLDER=uploads
# ALLOWED_EXTENSIONS=png,jpg,jpeg,gif
